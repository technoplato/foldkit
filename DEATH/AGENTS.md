# Agents | DEATH

This file does not replace the repo `AGENTS.md`. It is extra law for this folder.

1. The name is DEATH. Distributed Elm Architecture Teaching Hospital. Never DEF. Never ShrinkBench.
2. This folder is a temporary house. Do not treat it as a Foldkit public API.
3. File first, chat second. Answers go in `qanda.md` before they go in chat.
4. One question at a time. Do not dump the list in chat.
5. Source every new fork into `qanda.md` as a new Q at the end.
6. Read Michael's user messages in `sources/` before you PASS anything. Inter-bot mail is not his voice.
7. Do not invent a product path. Cite a path in this checkout or say you do not know.
8. Do not ping Grok Bot processes. Write `mailbox/to-grokbot/`. Read `mailbox/from-grokbot/`.
9. Grok Build on this Mac is the first tool. Computer use is last.
10. Speak in full sentences. Use the full analogy when you use an analogy. No "gate is open." No "did not sell as fact." No "cleared for you."
11. Messages fold through a sync engine as tape, then replay onto programs and surfaces.
12. The counter is the first way to participate. It is also an instance of the architecture, not the whole architecture.
13. Glanceable interview slides live in `examples/slides-qanda`. Deck JSON is `public/deck.json`. Answers land in `examples/slides-qanda/answers/<id>.md`. Fold answers into `DEATH/qanda.md`.
