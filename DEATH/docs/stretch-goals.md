# Stretch goals | DEATH

Ideas that are not version 1 law. Do not treat these as scored rungs.

| Goal | Why it waits | Source |
| --- | --- | --- |
| Host a fine-tuned model as part of a submit | Smaller instruction sets score better. A hosted fine-tune could replace a large prompt pack. That is a new host, a new license, and a new score row. | `examples/slides-qanda/answers/03d.json` |

When a stretch goal becomes a product hole, add a card in `examples/slides-qanda/public/deck.json` and a Q at the end of `qanda.md`.
