{
  "name": "replayability-core-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "prebuild": "pnpm --filter counter-core-example build && pnpm --filter counters-core-example build && pnpm --filter calculator-core-example build && pnpm --filter fact-core-example build",
    "pretest": "pnpm --filter counter-core-example build && pnpm --filter counters-core-example build && pnpm --filter calculator-core-example build && pnpm --filter fact-core-example build",
    "pretypecheck": "pnpm --filter counter-core-example build && pnpm --filter counters-core-example build && pnpm --filter calculator-core-example build && pnpm --filter fact-core-example build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "calculator-core-example": "workspace:*",
    "counter-core-example": "workspace:*",
    "counters-core-example": "workspace:*",
    "effect": "4.0.0-beta.97",
    "fact-core-example": "workspace:*",
    "foldkit": "workspace:*"
  },
  "devDependencies": {
    "@effect/platform-node": "4.0.0-beta.97",
    "prettier": "^3.8.4",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
