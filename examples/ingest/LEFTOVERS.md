# Ingest leftovers

- Wasm JS LLM is not fetched. Classifier inhabits `Deterministic`. `mapClassifierOutput` is the adapter that would map a live model onto the Category ADT. Do not inhabit `WasmReady` until a model loads.
- X bookmarks are not scraped. `XSession` starts `Unsigned`. The screen still paints. Capture X records a notice instead of blocking on login.
- yt-dlp is not wired from the browser host. `VideoTool` starts `Missing`. Capture video records a notice.
- Expo is a paint-only client (no workspace package.json), same as songbook.
