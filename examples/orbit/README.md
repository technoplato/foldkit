# Orbit

View-agnostic FoldKit example.
Play credits only. Boomerang is a spec.

Clients: foldkit, headless, cli, tui. Same core.
Scored challenge: FoldKit core + Three.js host (counters, then vending). Degree if 3JS only renders.
ASR: nofi / nofee / nophy / knophi = Knophy (knophy.com). Hosts: vending.knophy.com, store.knophy.com — not nofi.com.
Agent fetch: GET /api/agent /agent.md /api/catalog /api/chat /definitions.sh /prebuild-receipt.json
Continue at https://grok.com
