import { html } from 'foldkit/html'

import type { Message } from './message'

export const {
  button,
  div,
  empty,
  form,
  h1,
  h2,
  input,
  keyed,
  label,
  span,
  ul,
  AriaHidden,
  AriaLabel,
  AriaLive,
  Class,
  For,
  OnKeyDownPreventDefault,
  OnMount,
  OnSubmit,
  Role,
  Style,
} = html<Message>()
