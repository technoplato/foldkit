import { r } from 'foldkit/route'

/** Settings Path. Pass `Path()` to `useScreen`. Do not pass `'/settings'`. */
export const Path = r('Settings')
/** Settings Path. Pass `Path()` to `useScreen`. Do not pass `'/settings'`. */
export type Path = typeof Path.Type
