#!/bin/sh
ENV_FILE="${FOLDKIT_INSTANT_DEMO_ENV_FILE:-$HOME/.config/foldkit-instant-demo/instant.env}"
if [ -f "$ENV_FILE" ]; then
  set -a
  # shellcheck disable=SC1090
  . "$ENV_FILE"
  set +a
fi
exec "$@"
