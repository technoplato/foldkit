import { r } from 'foldkit/route'

/** Gate Path. Pass `Path()` to `useScreen`. Do not pass `'/gate'`. */
export const Path = r('Gate')
/** Gate Path. Pass `Path()` to `useScreen`. Do not pass `'/gate'`. */
export type Path = typeof Path.Type
