{
  "name": "wallet-live-client-example",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "prebuild": "pnpm --filter wallet-core-example build",
    "pretest": "pnpm --filter wallet-core-example build",
    "pretypecheck": "pnpm --filter wallet-core-example build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "2.22.1",
    "@scure/base": "1.2.6",
    "@scure/btc-signer": "2.2.0",
    "@solana-program/system": "0.13.0",
    "@solana/kit": "7.0.0",
    "effect": "4.0.0-beta.97",
    "viem": "2.55.10",
    "wallet-core-example": "workspace:*"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
