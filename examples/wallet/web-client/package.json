{
  "name": "wallet-web-client-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "prebuild": "pnpm --filter wallet-remote-example build && pnpm --filter wallet-simulated-client-example build",
    "pretest": "pnpm --filter wallet-remote-example build && pnpm --filter wallet-simulated-client-example build",
    "pretypecheck": "pnpm --filter wallet-remote-example build && pnpm --filter wallet-simulated-client-example build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@effect/platform-browser": "4.0.0-beta.97",
    "effect": "4.0.0-beta.97",
    "wallet-core-example": "workspace:*",
    "wallet-local-vault-example": "workspace:*",
    "wallet-remote-example": "workspace:*",
    "wallet-simulated-client-example": "workspace:*"
  },
  "devDependencies": {
    "happy-dom": "^20.10.4",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
