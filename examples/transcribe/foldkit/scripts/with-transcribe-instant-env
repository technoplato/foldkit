#!/bin/sh
ENV_FILE="${TRANSCRIBE_INSTANT_ENV:-$HOME/.config/foldkit-instant-demo/instant.env}"
if [ -f "$ENV_FILE" ]; then
  set -a
  # shellcheck disable=SC1090
  . "$ENV_FILE"
  set +a
fi
exec "$@"
