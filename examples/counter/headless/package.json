{
  "name": "counter-headless-example",
  "private": true,
  "version": "0.0.0",
  "description": "Headless Instant tail. From this dir: pnpm build (deps compile, no clean). From repo root: pnpm --filter counter-headless-example... build. Then pnpm counter-headless:instant. COUNTER_HEADLESS_TIME=human|ms|both|verbose. Burst: pnpm counter-headless:stress. Live Instant across every Host: COUNTER_LIVE_STRESS=1 pnpm counter-headless:live-stress.",
  "type": "module",
  "bin": {
    "foldkit-counter-headless": "./dist/entry.js"
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "compile": "tsc -p tsconfig.build.json",
    "counter-headless": "node dist/entry.js",
    "counter-headless:instant": "../scripts/with-counter-v01-env node dist/entry.js",
    "counter-headless:stress": "node dist/stress.js",
    "counter-headless:live-stress": "../scripts/with-counter-v01-env env COUNTER_LIVE_STRESS=1 COUNTER_TAPE=instant node dist/liveStress.js",
    "prebuild": "pnpm --filter @foldkit/instant compile && pnpm --filter counter-core-example compile",
    "pretypecheck": "pnpm --filter @foldkit/instant compile && pnpm --filter counter-core-example compile",
    "push:perms": "npx instant-cli@1.0.53 push perms --yes",
    "push:schema": "npx instant-cli@1.0.53 push schema --yes",
    "test": "pnpm run build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@foldkit/instant": "workspace:*",
    "counter-core-example": "workspace:*",
    "effect": "4.0.0-beta.97",
    "foldkit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "prettier": "^3.8.4",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
