---
'foldkit': minor
---

Compose `valid`, `screen`, and replay inspection into `Program.make`.
Foldkit paints the Program screen tree. `inspectReplayFrame` returns
Model, valid, and screen for every host.
