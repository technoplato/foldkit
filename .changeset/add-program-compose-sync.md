---
'foldkit': minor
'@foldkit/instant': minor
---

Add `Program.compose.sync` and `Runtime.start({ program, sync })`.
Instant has no Model. Hosts pass `Processor.Host` and two Schemas.
`Instant()` in `@foldkit/instant` is the live engine. `Memory()` is the fake.
