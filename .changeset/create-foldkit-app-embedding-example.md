---
'create-foldkit-app': minor
---

Add the `embedding` example: a Foldkit widget embedded in a plain TypeScript host page via `Runtime.embed`, with Flags in, typed Ports in both directions, and `dispose` on unmount.
