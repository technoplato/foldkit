---
'create-foldkit-app': minor
---

Rename the `upload` example to `interrupting-commands`. The scaffolded app is unchanged; only the example's name moved. Scripts passing `--example upload` should pass `--example interrupting-commands` instead.
