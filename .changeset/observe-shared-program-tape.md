---
'@foldkit/instant': minor
---

Export `observeRemoteAcceptedMessages` from `@foldkit/instant/sharing`.
Counter and Multiple Counters hosts fold history through Program
update, then apply only remote accepted Messages.
