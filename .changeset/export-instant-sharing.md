---
'@foldkit/instant': minor
---

Export `@foldkit/instant/sharing`. Counter CLI and Foldkit browser
are two Processors on one Instant account. The host writes each
Message before update and after update.
