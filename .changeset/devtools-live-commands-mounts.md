---
'foldkit': patch
---

DevTools' Commands and Mounts inspector tabs now reflect the latest entry in TimeTravel mode while Live. Previously they fell back to init state when no row was explicitly selected, even though the Model and Message tabs already showed the latest entry.
