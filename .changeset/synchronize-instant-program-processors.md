---
'@foldkit/instant': minor
---

Add authenticated, offline-first shared Program Processors with strict
append-only Message acceptance, recoverable pending proposals, transient
Processor rooms, capability-driven effect placement generations, inert replay,
and resumable artifact streams.
