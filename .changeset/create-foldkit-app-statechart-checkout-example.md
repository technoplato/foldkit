---
'create-foldkit-app': minor
---

Add the `statechart-checkout` example: a checkout workflow built on the experimental `foldkit/experimental/statechart` module, demonstrating guarded `when` branches and edge Commands.
