---
'foldkit': minor
'@foldkit/instant': patch
---

Add `Catalog`, `Interaction`, and `ActionMenu`. A Program that declares a Catalog gets derived buttons, keys, and CLI words through `program.interaction`, and `ActionMenu.compose` presents a Swift Navigation style action menu as a navigation destination. `Runtime.start` takes a session policy (`Mirror`, `SharedDomain`, or `Follow`) that decides which Processors apply each synced Message. Fix the Instant snapshot-log count decode for Effect 4 `Array.filterMap`.
