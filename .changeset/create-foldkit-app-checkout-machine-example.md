---
'create-foldkit-app': minor
---

Add the `checkout-machine` example: a checkout workflow built on the experimental `foldkit/experimental/machine` module, demonstrating guarded `when` branches and edge Commands.
