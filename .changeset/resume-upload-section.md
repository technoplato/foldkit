---
---

Internal test fixture refactor: replace `div` with explicit `Role('region')` with a semantic `<section>` in the `resumeUpload` scene test app, now that Scene resolves `<section>` with an accessible name to the implicit `region` role.
