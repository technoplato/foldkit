---
'foldkit': minor
---

Add a Program-bound Message admission definition that strictly decodes portable
claims, exposes their semantic occurrence identity, and resolves them against
the current Model with separate typed decode and resolution failures.
