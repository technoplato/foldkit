---
'foldkit': minor
'@foldkit/instant': patch
---

Use Instant admin subscribeQuery for live Node observe.
Add a Foldkit CLI adapter that starts a daemon on first invoke.
The non-captive CLI is a view of that Processor.
