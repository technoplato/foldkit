---
'@foldkit/instant': minor
---

Keep named-share Instant rooms (`-share-`) off the public tape the same
way owned `-mine-` rooms stay off public, so a kitchen increment does
not fold into the public count.
