---
'create-foldkit-app': minor
---

Add `canvas-art` to the scaffold prompt. Selecting it produces a project that uses `foldkit/canvas` to render shapes into a `<canvas>` element, with `Canvas.animationFrame` Subscriptions and pointer events wired up.
