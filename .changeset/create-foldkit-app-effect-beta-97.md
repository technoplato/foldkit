---
'create-foldkit-app': patch
---

Bump bundled Effect dependencies to `4.0.0-beta.97`. No user-facing changes. Newly scaffolded apps will get the updated pins from the example sources.

The CLI dependencies now pin `effect`, `@effect/platform-node`, and `@effect/platform-node-shared` to `4.0.0-beta.97` exactly during the v4 beta window.
