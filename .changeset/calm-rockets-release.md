---
'create-foldkit-app': patch
---

Bundle every scaffold source and the exact Foldkit package versions for the CLI's release instead of fetching examples from GitHub `main` and package versions from npm `latest`.
