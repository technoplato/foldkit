---
'foldkit': minor
---

Add `instrumentSignals` so a Program can classify Message streams as
Continuous, Periodic, or Discrete and retain bounded Model deltas beside
the domain Model without changing update return types.
